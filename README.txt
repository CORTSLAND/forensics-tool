**Tool that prevents unknown user's from accessing certain .jpg images. 
**Can be modified to include all file types.
**The file is rendered unreadable by the file systems. 
**The Python file only runs on linux distros for now.